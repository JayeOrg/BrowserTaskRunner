# Per-project vault tokens (preferred)
# VAULT_TOKEN_MY_PROJECT=<token from: npm run vault -- project export my-project>

# Optional
# SITE_CHECK_INTERVAL_MS=300000

# Docker / infrastructure (all have sensible defaults)
# ENABLE_VNC=true              # VNC server on port 5900 for visual debugging
# PERSIST_CHROME_PROFILE=false # Keep Chrome login sessions across runs
# DISPLAY_NUM=99               # Xvfb display number
# WS_PORT=8765                 # WebSocket port for extension ↔ browser communication
# SCREEN_SIZE=1280x720x24      # Virtual display resolution (WxHxDepth)
