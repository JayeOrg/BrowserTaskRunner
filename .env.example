# Per-project vault tokens (required — one per project)
# Export from vault: npm run vault -- project export <project-name>
# Naming: VAULT_TOKEN_<PROJECT> where PROJECT is project name uppercased with - → _
# Legacy fallback: VAULT_TOKEN (used when per-project key is missing — avoid with multiple projects)
VAULT_TOKEN_MONITOR_BOTC=<token from: npm run vault -- project export monitor-botc>
VAULT_TOKEN_NANDOS=<token from: npm run vault -- project export nandos>

# Optional
# SITE_CHECK_INTERVAL_MS=300000

# Docker / infrastructure (all have sensible defaults)
# ENABLE_VNC=true              # VNC server on port 5900 for visual debugging
# PERSIST_CHROME_PROFILE=false # Keep Chrome login sessions across runs
# DISPLAY_NUM=99               # Xvfb display number
# WS_PORT=8765                 # WebSocket port for extension ↔ browser communication
# SCREEN_SIZE=1280x720x24      # Virtual display resolution (WxHxDepth)
# READINESS_TIMEOUT=30         # Seconds to wait for Xvfb display readiness
